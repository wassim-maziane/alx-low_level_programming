#include "main.h"
/**
 * _strcmp - compares two strings
 * @s1: first string
 * @s2: second string
 *
 * Return:
 */
int _strcmp(char *s1, char *s2)
{
	int cnt = 0;
	while (*(s1 + cnt) == *(s1 + cnt))
		cnts++;
	if *(s

