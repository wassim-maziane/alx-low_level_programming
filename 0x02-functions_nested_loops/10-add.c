#include "main.h"
/**
 * add - adds two integers and returns the result
 * @a: first integer
 * @b: integer to be added to a
 *
 * Return: sum of a and b
 */
int add(int a, int b)
{
	return (a + b);
}
